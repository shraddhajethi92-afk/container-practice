FROM registry.access.redhat.com/ubi9/ubi

# Copy your repo definitions into the container
# (Assuming you keep them in the build context alongside this Containerfile)
COPY local.repo /etc/yum.repos.d/local.repo

# Copy the repo content from host into the container (optional, big size!)
# A better way: mount during build/run (see note below)
# COPY newrepo /mnt/newrepo

# Install httpd from local repo
RUN dnf -y install httpd && dnf clean all

EXPOSE 80
CMD ["/usr/sbin/httpd", "-DFOREGROUND"]